<div class="content col-md-12">
  <div class="content">
      <h1>My Pinned Ideas</h1>
  </div>
</div>  

<div class="content col-md-12">
  <ul class="list-group">
    <% @pins.each do |pin| %>
        <li class="list-group-item">

          <%= link_to idea_pin_path(pin.idea, pin), method: :delete, class: "btn btn-default btn-lg" do %>
            <span><i class="glyphicon glyphicon-pushpin" style="color:red"></i></span>
          <% end %>
            
          <span><%= link_to pin.idea.title, pin.idea %></span>

          <div class="ideas-list-buttons">
            <div class = "list-button">
              <button type="button" class="btn btn-lg" data-toggle="modal" data-target="#shareModal">
                <span class="fa fa-users"></span>
              </button>
            </div>
              
            <div class="list-button">
              <% if pin.idea.user == current_user %>
                <%= button_to idea_path(pin.idea), method: :delete, data: {confirm: "Are you sure you want to delete this idea?"}, class: "btn btn-lg" do %>
                  <span><i class="fa fa-trash-o"></i></span>
                <% end %>
              <% end %>
            </div>            
          </div>

        </li>
    <% end %>
  </ul>
</div>