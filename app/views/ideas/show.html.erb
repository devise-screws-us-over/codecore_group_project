<div class="container col-md-12">
 <div class="content">
   <button type="button" class="btn btn-lg">
     <%= link_to ideas_path do %>
       <i class="glyphicon glyphicon-menu-left"></i>
     <% end %>
   </button>
 </div>
 <div class="content"><h1><%= best_in_place @idea, :title, display_with: lambda { |t| markdown(t)} %></h1></div>

<<<<<<< HEAD
<div class="container col-md-12">
  <div>
    Created by <%= @idea.user.full_name %>
  </div>
  <div>
  <%= best_in_place @idea, :body, display_with: lambda { |t| markdown(t)}, as: :textarea %>
  </div>
   <%= button_to "Delete Idea", idea_path(@idea), method: :delete, data: {confirm: "Are you sure you want to delete this idea?"} %>
  </div>

  <div>
    <div class="comments">
      <%= render partial: "/comments/all_comment" %>
    </div>

    <div class="comment_form">
      <%= render partial: "/comments/comment" %>
    </div>
  </div>
=======
 <div class="content">
   <% pin = @idea.pins.find_by_user_id(current_user.id) %>
     <% if pin %>
       <%= link_to idea_pin_path(@idea, pin), method: :delete, class: "btn btn-lg" do %>
       <i class="glyphicon glyphicon-pushpin" style="color:red"></i><% end %>
     <% else %>
       <%= link_to idea_pins_path(@idea), method: :post, class: "btn btn-lg" do %>
       <i class="glyphicon glyphicon-pushpin" style="color:green"></i><% end %>
     <% end %>
 </div>
 <%#--from CANCANCAN ----------- %>
 <% if can? :edit, @idea %>
   <div class="content"><%= link_to " Edit", edit_idea_path(@idea), class: "btn btn-default fa fa-pencil" %></div>
 <% end %>

 <div>
   Created by <%= @idea.user.full_name %>
 </div>
 <br>
 <div>
 <%= best_in_place @idea, :body, display_with: lambda { |t| markdown(t)}, as: :textarea, :html_attrs => { :style => 'width:55%;' } %>
 </div>
<br>
<div class="row">
  <%= button_to "Delete Idea", idea_path(@idea), method: :delete, data: {confirm: "Are you sure you want to delete this idea?"} %>
 </div>
<br>
</div>
 <h3>Comments:</h3>
 <div class="comments">
 <%= render partial: "/comments/all_comment" %>
 </div>

<div class="comment_form">
 <%= render partial: "/comments/comment" %>
</div>
>>>>>>> 99587384c13e97bafab655f6a36417da5744f663
</div>
