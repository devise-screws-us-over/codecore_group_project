<%= simple_form_for @comment, url: idea_comments_path(@idea), remote: true do |f| %>
  <%= f.text_area :body, class: "form-control" %>
  <br>
  <%= f.submit "Submit Comment", class: "btn btn-primary" %> 
<% end %>